packages:
  - "apps/*"
  - "packages/*"
  - "tools/*"

# tsx ships esbuild, which needs its postinstall to fetch the platform binary.
onlyBuiltDependencies:
  - esbuild

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
