# The fill tool is the only process that talks to DeepSeek; the game server
# never does. If this file does not exist, apps/game/.env is read instead, so
# one key in one place is enough for local development.
DEEPSEEK_API_KEY=sk-...
# Optional. Defaults to deepseek-v4-pro; deepseek-v4-flash is the cheaper option.
# DEEPSEEK_MODEL=deepseek-v4-flash

# Optional. The vision model every downloaded picture is shown to before it
# is banked — does it show the subject, is the answer written on it. Defaults
# to deepseek-v4-flash-vision-exp on the same key; set it to nothing at all
# (DEEPSEEK_VISION_MODEL=) to switch the check off.
# DEEPSEEK_VISION_MODEL=deepseek-v4-flash-vision-exp

# Optional. Image search across the whole web, as Google Images sees it,
# through serper.dev — the source for gameplay screenshots of console games,
# frames from films and logos without the wordmark. Without it the lookup is
# Wikimedia and the Steam store alone. Sign up at serper.dev (a few thousand
# free queries, no card), copy the key. Fractions of a cent a query after.
# SERPER_API_KEY=

# Optional. How hard the model thinks per round: none, low, high or max.
# Defaults to low — the thinking is most of a round's latency and cost, and
# the fill loop's validation-and-retry is what guards quality. Raise it if
# the log shows rejects climbing; "none" turns thinking off entirely.
# DEEPSEEK_REASONING_EFFORT=low

# The Postgres the round bank lives in. Must be the SAME database the game
# server reads — the whole point is filling its shelves. If unset here, the
# value in apps/game/.env is used, like the API key above.
# DATABASE_URL=postgresql://user:password@127.0.0.1:5432/guessly

# The bucket the round bank's pictures go in. Must be the SAME bucket the game
# server reads — filling any other one is pouring water beside the glass. If
# unset here, the values in apps/game/.env are used, like the API key above.
# S3_ENDPOINT=https://s3.example.com
# S3_BUCKET=guessly-images
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_REGION=garage
